// Tailwind v4 — la configurazione vive nel CSS (@theme).
// Qui solo il collegamento del plugin PostCSS.
const config = {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};

export default config;
